# KYA runtime
NODE_ENV=development
PORT=8787
PUBLIC_BASE_URL=http://localhost:8787
KYA_ISSUER=http://localhost:8787
KYA_AUDIENCE=kya-agent
KYA_DATA_DIR=.kya-data
CREDENTIAL_TTL_SECONDS=900
CHALLENGE_TTL_SECONDS=120
KYC_TTL_DAYS=365

# Payments (Mandate + Yuno mock) — placeholder rule, refine once the real rule is defined.
MANDATE_MAX_AMOUNT=500
# Mock Yuno endpoint (parallel dev). Unset = local synthetic stub response.
YUNO_MOCK_URL=
